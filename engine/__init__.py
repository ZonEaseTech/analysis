"""Reporting engine (cross-layer infrastructure).

Future home of:
  report_engine.py     ← utils/report_engine.py
  resource_adapter.py  ← utils/resource_adapter.py

Stays out of `semantic/` because it knows about Excel/concurrency, not
business meaning.
"""
