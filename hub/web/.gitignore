node_modules
dist
*.local
.DS_Store

# The repo-root .gitignore has a Python rule `lib/` that false-matches this
# TS frontend's src/shared/lib dir — re-include it so its sources are tracked.
!src/shared/lib/

# generated by TanStack Router plugin
src/app/routeTree.gen.ts
